diff -Nru icedtea6-1.1.orig/ChangeLog icedtea6-1.1/ChangeLog
--- icedtea6-1.1.orig/ChangeLog	2008-04-04 14:56:04.000000000 +0100
+++ icedtea6-1.1/ChangeLog	2008-06-01 19:16:31.000000000 +0100
@@ -1,3 +1,7 @@
+2008-05-30  Mark Wielaard  <mark@klomp.org>
+
+	* patches/icedtea-ecj.patch: Use `cat file-list` not gjar -@ stdin.
+
 2008-04-04  Gary Benson  <gbenson@redhat.com>
 
 	* patches/icedtea-ia64-bugfix.patch: New file.
diff -Nru icedtea6-1.1.orig/patches/icedtea-ecj.patch icedtea6-1.1/patches/icedtea-ecj.patch
--- icedtea6-1.1.orig/patches/icedtea-ecj.patch	2008-03-12 19:55:19.000000000 +0000
+++ icedtea6-1.1/patches/icedtea-ecj.patch	2008-06-01 19:16:14.000000000 +0100
@@ -665,9 +665,9 @@
  	$(prep-target)
 -	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
 -	    @$(RES_JAR_ARGLIST) $(JAR_JFLAGS)
-+	(cd $(CLASSBINDIR) && $(CAT) $(RES_JAR_FILELIST) | \
-+	$(BOOT_JAR_CMD) c0mf@ $(JAR_MANIFEST_FILE) $@ \
-+	    $(JAR_JFLAGS))
++	(cd $(CLASSBINDIR) && \
++	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
++	    $(JAR_JFLAGS) `cat $(RES_JAR_FILELIST)`)
  	@$(java-vm-cleanup)
  
  # Create jsse.jar containing SunJSSE implementation classes
@@ -680,9 +680,9 @@
  	$(prep-target)
 -	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
 -	    @$(RT_JAR_ARGLIST) $(JAR_JFLAGS)
-+	(cd $(CLASSBINDIR) && $(CAT) $(RT_JAR_FILELIST) | \
-+	$(BOOT_JAR_CMD) c0mf@ $(JAR_MANIFEST_FILE) $@ \
-+	    $(JAR_JFLAGS))
++	(cd $(CLASSBINDIR) && \
++	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
++	    $(JAR_JFLAGS) `$(CAT) $(RT_JAR_FILELIST)`)
  	@$(java-vm-cleanup)
  
  # Meta-index construction to make core class loaders lazier
