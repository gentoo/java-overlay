diff -Nru icedtea6-1.3.1.old/Makefile.am icedtea6-1.3.1/Makefile.am
--- icedtea6-1.3.1.old/Makefile.am	2008-12-02 16:28:48.000000000 +0000
+++ icedtea6-1.3.1/Makefile.am	2008-12-02 16:35:31.000000000 +0000
@@ -1396,6 +1396,9 @@
 if !USE_SYSTEM_CACAO
 	cd cacao/cacao && \
 	./configure \
+	  --host=$(host_alias) \
+	  --build=$(build_alias) \
+	  --target=$(target_alias) \
 	  --prefix=$(abs_top_builddir)/cacao/install \
 	  --with-java-runtime-library=openjdk \
 	  --with-java-runtime-library-prefix=$(abs_top_builddir)/openjdk \
