diff -Nru icedtea6-1.2.orig/ChangeLog icedtea6-1.2/ChangeLog
--- icedtea6-1.2.orig/ChangeLog	2008-05-28 16:00:53.000000000 +0100
+++ icedtea6-1.2/ChangeLog	2008-06-01 19:14:56.000000000 +0100
@@ -1,3 +1,7 @@
+2008-05-30  Mark Wielaard  <mark@klomp.org>
+
+	* patches/icedtea-ecj.patch: Use `cat file-list` not gjar -@ stdin.
+
 2008-05-28  Lillian Angel  <langel@redhat.com>
 
 	* NEWS: Updated for release.
diff -Nru icedtea6-1.2.orig/patches/icedtea-ecj.patch icedtea6-1.2/patches/icedtea-ecj.patch
--- icedtea6-1.2.orig/patches/icedtea-ecj.patch	2008-03-12 19:55:19.000000000 +0000
+++ icedtea6-1.2/patches/icedtea-ecj.patch	2008-06-01 19:14:43.000000000 +0100
@@ -665,9 +665,9 @@
  	$(prep-target)
 -	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
 -	    @$(RES_JAR_ARGLIST) $(JAR_JFLAGS)
-+	(cd $(CLASSBINDIR) && $(CAT) $(RES_JAR_FILELIST) | \
-+	$(BOOT_JAR_CMD) c0mf@ $(JAR_MANIFEST_FILE) $@ \
-+	    $(JAR_JFLAGS))
++	(cd $(CLASSBINDIR) && \
++	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
++	    $(JAR_JFLAGS) `cat $(RES_JAR_FILELIST)`)
  	@$(java-vm-cleanup)
  
  # Create jsse.jar containing SunJSSE implementation classes
@@ -680,9 +680,9 @@
  	$(prep-target)
 -	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
 -	    @$(RT_JAR_ARGLIST) $(JAR_JFLAGS)
-+	(cd $(CLASSBINDIR) && $(CAT) $(RT_JAR_FILELIST) | \
-+	$(BOOT_JAR_CMD) c0mf@ $(JAR_MANIFEST_FILE) $@ \
-+	    $(JAR_JFLAGS))
++	(cd $(CLASSBINDIR) && \
++	$(BOOT_JAR_CMD) c0mf $(JAR_MANIFEST_FILE) $@ \
++	    $(JAR_JFLAGS) `$(CAT) $(RT_JAR_FILELIST)`)
  	@$(java-vm-cleanup)
  
  # Meta-index construction to make core class loaders lazier
