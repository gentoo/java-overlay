--- t/velocity-1.4/build/build.xml	2005-08-12 22:13:09.000000000 +0200
+++ build.xml	2007-02-26 00:46:01.766125275 +0100
@@ -22,7 +22,7 @@
 
   <property name="src.java.dir" value="../src/java"/>
   <property name="javadoc.destdir" value="../docs/api"/>
-  <property name="final.name" value="${project}-${version}"/>
+  <property name="final.name" value="${project}"/>
   <property name="dist.root" value="../dist"/>
   <property name="dist.dir" value="${dist.root}/${final.name}"/>
   <property name="year" value="2002"/>
@@ -270,7 +270,7 @@
  
     <delete dir="${build.dest}/meta-inf" quiet="true"/>
 
-    <jar jarfile="${build.dir}/${project}-dep-${version}.jar">
+    <jar jarfile="${build.dir}/${project}-dep.jar">
 
       <fileset dir="${build.dest}">
         <exclude name="**/package.html"/>
@@ -320,7 +320,7 @@
   <!-- =================================================================== -->
   <target name="jar" depends="compile">
     
-    <jar jarfile="${build.dir}/${project}-${version}.jar">
+    <jar jarfile="${build.dir}/${project}.jar">
       <manifest>
          <attribute name="Built-By" value="JBoss Inc."/>
          <attribute name="Created-by" value="${java.vm.version} ${java.vendor}"/>
@@ -354,7 +354,7 @@
 
     <delete dir="${build.dest}/meta-inf" quiet="true"/>
 
-    <jar jarfile="${build.dir}/${project}-J2EE-dep-${version}.jar"
+    <jar jarfile="${build.dir}/${project}-J2EE-dep.jar"
       basedir="${build.dest}"
       excludes="**/package.html"
     />
@@ -368,7 +368,7 @@
   <!-- =================================================================== -->
   <target name="jar-J2EE" depends="compile-J2EE" if="J2EE.present">
         
-    <jar jarfile="${build.dir}/${project}-J2EE-${version}.jar"
+    <jar jarfile="${build.dir}/${project}-J2EE.jar"
       basedir="${build.dest}"
       excludes="**/package.html"
     />
@@ -380,7 +380,7 @@
   <!-- =================================================================== -->
   <target name="jar-core" depends="compile">
         
-    <jar compress="true" jarfile="${build.dir}/${project}-core-${version}.jar">
+    <jar compress="true" jarfile="${build.dir}/${project}-core.jar">
       <fileset dir="${build.dest}">
         <exclude name="**/package.html"/>
         <exclude name="org/apache/oro/**"/>
@@ -400,7 +400,7 @@
   <!-- =================================================================== -->
   <target name="jar-util" depends="compile">
         
-    <jar jarfile="${build.dir}/${project}-util-${version}.jar">
+    <jar jarfile="${build.dir}/${project}-util.jar">
       <fileset dir="${build.dest}">
         <exclude name="**/package.html"/>
         <include name="org/apache/velocity/anakia/**"/>
@@ -417,7 +417,7 @@
   <!-- =================================================================== -->
   <target name="jar-servlet" depends="compile">
         
-    <jar jarfile="${build.dir}/${project}-servlet-${version}.jar">
+    <jar jarfile="${build.dir}/${project}-servlet.jar">
       <fileset dir="${build.dest}">
         <exclude name="**/package.html"/>
         <include name="org/apache/velocity/servlet/**"/>
@@ -608,8 +608,8 @@
     />
 
     <copy
-      file="${build.dir}/${project}-dep-${version}.jar"
-      tofile="${dist.dir}/${project}-dep-${version}.jar"
+      file="${build.dir}/${project}-dep.jar"
+      tofile="${dist.dir}/${project}-dep.jar"
     />
 
     <fixcrlf srcdir="${dist.dir}"
@@ -735,7 +735,7 @@
           classname="org.apache.velocity.anakia.AnakiaTask">
           
           <classpath>
-             <pathelement location="${build.dir}/${project}-${version}.jar"/>
+             <pathelement location="${build.dir}/${project}.jar"/>
              <path refid="classpath"/>
           </classpath>
 
@@ -798,7 +798,7 @@
           classname="org.apache.velocity.anakia.AnakiaTask">
           
           <classpath>
-             <pathelement location="${build.dir}/${project}-${version}.jar"/>
+             <pathelement location="${build.dir}/${project}.jar"/>
              <path refid="classpath"/>
           </classpath>
 
